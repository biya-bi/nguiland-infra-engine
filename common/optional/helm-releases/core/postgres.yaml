apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgres
  namespace: infra
spec:
  interval: 10m
  dependsOn:
    - name: chart-repository
      namespace: infra
  chart:
    spec:
      chart: postgres
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: chart-repository
  values:
    replicaCount: 1
