- op: add
  path: /spec/rules/-
  value:
    host: ${ACTIFACTORY_OSS_HOST}
    http:
      paths:
        - backend:
            service:
              name: artifactory-oss
              port:
                number: 8082
          path: /
          pathType: Prefix
