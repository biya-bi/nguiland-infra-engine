apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: io-utils
  namespace: infra
spec:
  interval: 1m0s
  ref:
    branch: develop # In development clusters, the source-controller should clone the develop branch
  secretRef:
    name: ssh-credentials
  url: ssh://github.com/biya-bi/rainbow-io-utils.git
