apiVersion: v1
kind: ConfigMap
metadata:
  name: env-settings
  namespace: infra
data:
  environment: ${ENVIRONMENT}
  image-pull-endpoint: ${IMAGE_PULL_ENDPOINT}
  image-push-endpoint: ${IMAGE_PUSH_ENDPOINT}
