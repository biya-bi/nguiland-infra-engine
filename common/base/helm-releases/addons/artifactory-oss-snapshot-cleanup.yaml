apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: artifactory-oss-snapshot-cleanup
  namespace: infra
spec:
  suspend: true
  interval: 10m
  dependsOn:
    - name: artifactory-oss
      namespace: infra
  chart:
    spec:
      chart: artifactory-oss-snapshot-cleanup
      version: "0.1.0"
      sourceRef:
        kind: HelmRepository
        name: artifactory-oci
      interval: 10m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    imagePullEndpoint: artifactory-jcr.infra
    artOssUrl: http://artifactory-oss:8082/artifactory
