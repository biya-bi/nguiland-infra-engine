apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keycloak
  namespace: infra
spec:
  interval: 10m
  chart:
    spec:
      chart: ./core/keycloak
      sourceRef:
        kind: GitRepository
        name: keycloak
      interval: 10m
  values:
    replicaCount: 1
