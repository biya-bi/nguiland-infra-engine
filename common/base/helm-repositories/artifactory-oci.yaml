apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: artifactory-oci
  namespace: infra
spec:
  type: oci
  url: oci://${ACTIFACTORY_JCR_HOST}:${ACTIFACTORY_JCR_PORT}/docker-local
  interval: 5m
  secretRef:
    name: docker-config
