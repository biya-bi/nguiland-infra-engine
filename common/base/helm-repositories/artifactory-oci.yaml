apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: artifactory-oci
  namespace: ${ENVIRONMENT}
spec:
  type: oci
  url: oci://${ARTIFACTORY_JCR_HOST}:${ARTIFACTORY_JCR_PORT}/docker-local
  interval: 5m
  secretRef:
    name: docker-config
