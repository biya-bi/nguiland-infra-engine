apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgres
  namespace: infra
spec:
  interval: 10m
  chart:
    spec:
      chart: ./core/postgres
      sourceRef:
        kind: GitRepository
        name: postgres
      interval: 10m
