apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: helm-releases
  namespace: infra
spec:
  interval: 10m0s
  path: ./bootstrap/dev/helm-releases
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
